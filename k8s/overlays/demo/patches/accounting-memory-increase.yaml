apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounting
  namespace: otel-demo
spec:
  template:
    spec:
      containers:
        - name: accounting
          resources:
            limits:
              memory: 256Mi
