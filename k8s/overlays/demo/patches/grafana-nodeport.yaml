apiVersion: v1
kind: Service
metadata:
  name: grafana-nodeport
  namespace: otel-demo
spec:
  type: NodePort
  ports:
    - name: service
      port: 3000
      targetPort: grafana
      nodePort: 30380
  selector:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: otel-demo
